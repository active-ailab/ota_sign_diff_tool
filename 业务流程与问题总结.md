# 业务流程与问题总结

## 1. 项目概述

当前项目是一个基于 Flask 的版本差分包工具，核心目标是把不同来源的 OTA 签名包整理成可发布的差分包，并把结果上传到 NAS，最后在网页端返回可复制的下载链接。

当前界面提供三类能力：

- NAS 链接差分：输入两个 NAS 分享链接，自动下载后生成差分包
- 版本号差分：输入项目名和两个版本号，自动从 NAS 版本目录中查找对应包并生成差分包
- 本地上传差分：直接上传两个本地总包，由系统自动提取 `watch@<芯片名>_ota_sign.zip` 后生成差分包

项目早期还尝试过通过飞书机器人发送通知，但在企业内网环境下遇到了一系列权限、穿透和 SDK 加载问题，最终放弃自动推送，改为在页面直接展示 NAS 链接并提供复制按钮。这样做更稳定，也更适合当前网络环境。

---

## 2. 业务流程

### 2.1 总体流程

```text
用户在页面选择差分模式
    -> 填写必要参数
    -> 提交任务
    -> Flask 后端创建后台任务
    -> 下载或接收原始 OTA 包
    -> 提取目标 ota_sign 包
    -> 解析版本信息
    -> 调用差分生成逻辑
    -> 生成差分 OTA 包
    -> 上传 NAS 并获取分享链接
    -> 前端展示文件名和链接
```

### 2.2 NAS 链接差分流程

1. 用户粘贴旧版本和新版本的 NAS 分享链接。
2. 后端从链接中识别真实文件名和项目名。
3. 系统下载两个原始包，并显示下载进度。
4. 系统递归查找并提取 `watch@<芯片名>_ota_sign.zip`。
5. 系统解析版本号，生成差分包。
6. 差分包上传 NAS，生成分享链接。
7. 前端展示结果，用户可直接复制。

### 2.3 版本号差分流程

1. 用户输入项目名、基线版本和目标版本。
2. 后端根据项目名和版本号定位 NAS 目录。
3. 下载对应版本的包到临时目录。
4. 提取 ota_sign 包。
5. 调用差分生成器生成差分包。
6. 上传 NAS 并返回分享链接。
7. 页面显示差分包名称和 NAS 链接。

### 2.4 本地上传差分流程

1. 用户选择本地旧包和新包。
2. 前端先做文件名快速猜测，减少等待感。
3. 提交后，浏览器先显示上传进度。
4. 后端接收文件后递归扫描压缩包。
5. 系统定位 `watch@<芯片名>_ota_sign.zip` 并提取出来。
6. 系统生成差分包，再上传到 NAS。
7. 前端显示文件名和分享链接。

---

## 3. 核心实现思路

### 3.1 后端职责

后端主要负责三件事：

- 任务管理：创建任务、维护状态、保存日志、返回结果
- 文件处理：下载、解压、递归扫描、版本识别、差分生成
- 结果交付：上传 NAS、生成分享链接、通知前端和消息系统

### 3.2 前端职责

前端主要负责三件事：

- 按模式收集输入参数
- 显示进度和日志
- 展示最终结果并支持复制链接

### 3.3 任务模型

系统把每次提交都作为独立任务处理，任务通常包含：

- 任务 ID
- 模式类型
- 当前状态
- 进度百分比
- 日志列表
- 差分包路径
- NAS 分享链接

这种方式的好处是任务执行和页面刷新分离，界面不会因为长时间处理而卡死。

---

## 4. 开发中遇到的问题总结

### 4.1 服务器申请

- 业务需求：本地开发的工具脚本已完成全功能测试，为实现团队全员共享使用，需将代码部署至服务器上线运行。
- 原方案问题：计划通过内网 GitLab 完成项目部署，并发布到腾讯云广州服务器；但该应用需对接合肥内网 NAS 服务器，由于 NAS 为内网隔离环境，仅允许内网访问，外网服务器无法打通访问链路，导致项目部署后无法正常使用 NAS 资源。
- 解决方案：特向 IT 部门申请一台合肥内网 Windows Server 虚拟服务器（具备联网权限），直接在该内网虚拟机上完成项目的部署与运行，确保应用可正常访问内网 NAS，满足工具的全员使用需求。 

### 4.2 飞书通知方案在内网环境下不可持续
项目复盘：飞书机器人推送方案踩坑与替代方案
- 方案背景
项目初期规划：通过飞书机器人将差分结果自动推送给任务触发人；
核心目标：实现飞书用户接收专属个人通知；
方案核心依赖：调用飞书容器内 JSAPI 自动获取用户身份（open_id/user_id）。
- 致命根因（身份获取失败唯一核心问题）
飞书对容器内网页访问地址有强制安全限制，直接导致身份获取功能完全失效：
当前访问地址：10.2.7.239:5000（内网 IP + HTTP 协议），属于飞书禁用的地址格式；
直接后果：飞书容器全局禁用 JSAPI 功能 → 无法获取授权 code → 身份校验接口完全不可用；
官方硬性要求：必须使用 HTTPS 协议 + 公网域名 访问，否则无法实现身份获取。
- 企业内网核心踩坑总结
内网穿透完全失效：飞书开发者工具自带的/opdev内网穿透命令，在内网环境下彻底不可用；
第三方工具被封杀：cpolar、ngrok 等通用穿透工具，被公司防火墙拦截，无法连接 / 无响应； 
资源加载异常：外网 CDN 在内网无法访问，飞书 H5 SDK 必须改为本地引入。
- 终极解决方案
放弃飞书机器人推送及身份校验相关调试，采用极简可靠方案：
内网 IP 直接调试 + 页面展示 NAS 链接 + 一键复制按钮  

### 4.3 测试中遇到兼容问题，需要兼容多个平台 
    watch@mhs003_ota_sign.zip、
    watch@mhs003s_ota_sign.zip、
    watch@apollo4_ota_sign.zip、
    watch@mimxrt595_ota_sign.zip。 

- 支持黄山3  黄山3s apollo4  mimxrt595 四个平台
 

### 4.4 上传总包后，用户不知道系统在做什么

很多慢操作其实发生在：

- 上传文件到服务器
- 解压多个压缩层
- 递归查找目标 ota_sign 包

如果这些阶段不打印清晰日志，用户就会感觉系统卡住。后来增加了更明确的阶段性输出，例如：

- 正在上传
- 正在扫描压缩包
- 正在定位 ota_sign
- 正在生成差分包
- 正在上传 NAS

 
 

### 4.7 结果字段回传不一致

曾经出现过“任务成功了，但页面没有显示 NAS 文件名和链接”的情况。原因是：

- 后端对差分输出路径的日志格式依赖过强
- 实际输出信息格式变化后，结果路径没有被正确提取

后续改成：

- 同时兼容多种日志格式
- 在找不到日志路径时使用已知输出文件兜底
- 保证任务成功后一定能返回可展示的结果字段

### 4.8 递归解包过程缺少可解释性

本地上传和 NAS 下载都存在多层压缩包场景。若没有过程日志，用户难以判断系统是否真的在工作。

后来补充了以下可读性日志：

- 当前扫描到第几层压缩包
- 是否发现嵌套压缩包
- 是否命中 `watch@<芯片名>_ota_sign.zip`
- 最终定位到哪个文件


---

## 5. 当前项目的经验总结

### 5.1 体验比“纯正确”更重要

对这种长耗时工具来说，用户体感往往比内部状态更重要。只要界面没有反馈，用户就会认为系统有问题。

### 5.2 日志要分层

日志最好分成两类：

- 面向普通用户的阶段日志
- 面向开发排障的详细日志

两者混在一起，会让页面既吵又难用。

### 5.3 任务结果必须可回传

不管任务内部经历了多少步骤，最终都要稳定返回：

- 差分包名称
- NAS 分享链接
- 成功/失败状态

否则前端即使显示成功，也会出现结果区空白的问题。

### 5.4 模式逻辑要统一管理

当前项目有多个 tab，如果每个 tab 自己维护一套逻辑，很容易互相干扰。统一的配置表和统一的提交入口更可靠。

---

## 6. 当前项目的输出形式

任务成功后，页面应至少展示以下内容：

- 差分包文件名
- NAS 分享链接
- 可复制按钮
- 任务进度已完成

对于普通用户来说，这些信息已经足够，不需要看到差分算法内部细节。

---

## 7. 后续可继续优化的方向

- 把用户可见日志继续压缩成更少的阶段提示
- 增加更明确的错误分类，方便排障
- 给不同模式补充更清晰的空状态提示
- 为长任务增加更细粒度的前端状态提示
- 给核心流程补一个简版 README，方便新同事快速上手

---

## 8. 结论

这个项目的本质不是单纯的“生成差分包”，而是一个围绕差分包生成、上传和结果分发的完整流程工具。当前已经把核心链路打通，接下来最重要的工作是继续提升任务反馈、日志可读性和结果展示稳定性。
